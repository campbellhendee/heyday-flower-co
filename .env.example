## Copy this to `.env.local` for local dev, or add it in Vercel Project Settings → Environment Variables
## Used for canonical URLs (metadata, sitemap, robots)
NEXT_PUBLIC_SITE_URL=https://your-domain-or-vercel-url.vercel.app

